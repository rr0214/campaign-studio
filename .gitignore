.env
__pycache__/
*.pyc
.DS_Store
chroma_db/
evals/eval_results.csv
evals/brand_safety_eval_results.csv
# Ignore Streamlit secrets, but let the pinned theme travel with the repo —
# the app must look the same on any machine, not inherit the viewer's OS theme.
.streamlit/*
!.streamlit/config.toml
